"use strict";

(function (Notes) {
    Notes.viewModel.noteLinesCountViewModel = function(options) {
        var that = {};

        // TODO
        that.setNoteClientId = function () {};

        return that;
    };
})(Notes);
